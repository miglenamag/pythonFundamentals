# version = [int(number) for number in input().split(".")]

# my_list = input().split(".")
# version = list(map(int, my_list))
# print(version)

version = list(map(int, input().split(".")))
print(version)